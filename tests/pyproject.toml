[project]
name = "docindex-tests"
version = "0.0.0"
description = "End-to-end pytest harness for docindex-server."
requires-python = ">=3.11"
dependencies = [
    "pytest>=8",
    "httpx>=0.27",
]

[tool.pytest.ini_options]
testpaths = ["suites"]
addopts = "-ra"
markers = [
    "smoke: phase 1 startup smoke tests",
    "e2e: phase 2 end-to-end http tests",
]
